name: shared matrix
on:
  push:
  workflow_dispatch:

jobs:
  list-environments:
    runs-on: ubuntu-latest

    outputs:
      colors: ${{ steps.colors.outputs.colors }}
      listenvs: ${{ steps.colors.outputs.listenvs}}

    steps:
      - name: Define Colors
        id: colors
        run: |
          lists="$(gh api repos/$OWNER/$REPO/environments|jq -c '[.environments[].name|tojson]')"
          echo 'colors=["red", "green", "blue"]' >> "$GITHUB_OUTPUT"
          echo 'listenvs='$lists >> "$GITHUB_OUTPUT"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          OWNER: ${{ github.repository_owner }}
          REPO: ${{ github.event.repository.name }}

  deploy-application:
    runs-on: ubuntu-latest
    needs:
    - list-environments
    strategy:
      matrix:
        environments: ${{ fromJSON(needs.list-environments.outputs.listenvs) }}

    steps:
    - name: Report Env
      run: |
        cat ${{ APPLICATION_NAME}}
